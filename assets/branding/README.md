# Branding sources

`aion3-logo-source.jpeg` preserves the original logo source formerly stored in `img/aionelogo.jpeg`.
The application serves its production logo from `apps/management/public/img/`; changing the source
asset alone does not replace the served logo.
